# Example environment variables for the replication
# Copy this file to .env and fill values before running notebooks

# GitHub API token with repo access (used to fetch PR metadata and stats)
GITHUB_TOKEN=ghp_replace_with_your_token